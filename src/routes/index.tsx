import { component$ } from "@builder.io/qwik";
import { LootTable } from "~/components/loot-table/loot-table";

export default component$(() => {

  return (
    <div>
      <LootTable />
    </div>
  );
});
